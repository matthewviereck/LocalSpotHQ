# LocalSpot HQ - canonical host + legacy redirects

RewriteEngine On

# Force HTTPS and www (canonical host is https://www.localspothq.com)
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^localspothq\.com$ [NC]
RewriteRule ^(.*)$ https://www.localspothq.com/$1 [R=301,L]

# Legacy single-page app -> Phoenixville area app
RewriteRule ^app\.html$ https://www.localspothq.com/phoenixville/ [R=301,L]

# Calendar feed MIME type
AddType text/calendar .ics

# No JSON in the docroot is meant to be public (submissions/subscriber data
# lives above the docroot, but deny defensively in case a file reappears).
<FilesMatch "\.(json|txt)$">
    Require all denied
</FilesMatch>

# robots.txt must stay reachable despite the .txt deny above
<Files "robots.txt">
    Require all granted
</Files>

# Legacy submission log directory
RewriteRule ^logs(/|$) - [F,L]
